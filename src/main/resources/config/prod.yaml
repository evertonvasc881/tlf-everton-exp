# tlf-everton-exp — PROD environment properties
http:
  port: 8081

tlf:
  sales:
    sys:
      host: "tlf-sales-sys-prod.cloudhub.io"
      port: 443
      basepath: "/com/v1"
      protocol: "HTTPS"
      connectionTimeout: 10000
      responseTimeout: 30000
