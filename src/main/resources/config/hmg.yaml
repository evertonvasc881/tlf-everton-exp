# tlf-everton-exp — HMG environment properties
http:
  port: 8081

tlf:
  sales:
    sys:
      host: "tlf-sales-sys-hmg.cloudhub.io"
      port: 443
      basepath: "/com/v1"
      protocol: "HTTPS"
      connectionTimeout: 10000
      responseTimeout: 30000
